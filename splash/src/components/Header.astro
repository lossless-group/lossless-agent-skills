---
import ModeToggle from './ModeToggle.astro';
import SearchBox from './SearchBox.astro';
const base = import.meta.env.BASE_URL;
---

<header class="site-header">
  <div class="container site-header__inner">
    <a class="site-header__brand" href={base}>
      <span class="brand__mark mono" aria-hidden="true">$_</span>
      <span class="brand__name">Lossless Agent Skills</span>
      <span class="brand__short mono" aria-hidden="true">LAS</span>
    </a>
    <nav class="site-header__nav" aria-label="Primary">
      <a href={`${base}skills/`}>Skills</a>
      <a href={`${base}changelog/`}>Changelog</a>
      <a href={`${base}search/`}>Search</a>
      <a
        href="https://github.com/lossless-group/lossless-agent-skills"
        target="_blank"
        rel="noopener"
      >
        GitHub
      </a>
    </nav>
    <div class="site-header__actions">
      <SearchBox compact />
      <ModeToggle />
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in oklab, var(--color-bg) 90%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--color-border);
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    height: 60px;
  }

  .site-header__actions {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
  }

  .site-header__brand {
    display: inline-flex;
    align-items: baseline;
    gap: var(--space-2);
    color: var(--color-text);
    font-family: var(--font__display);
    font-weight: 600;
    font-size: 0.95rem;
    letter-spacing: -0.005em;
  }
  .site-header__brand:hover { text-decoration: none; }

  .brand__mark {
    color: var(--color-accent);
    font-size: 1rem;
    line-height: 1;
    font-weight: 700;
  }
  .brand__short {
    display: none;
    font-size: 0.74rem;
    color: var(--color-text-dim);
    letter-spacing: 0.12em;
  }

  @media (max-width: 720px) {
    .brand__name { display: none; }
    .brand__short { display: inline; }
  }

  .site-header__nav {
    display: flex;
    align-items: center;
    gap: var(--space-5);
    margin-left: auto;
    font-family: var(--font__mono);
    font-size: 0.84rem;
    letter-spacing: 0.02em;
  }
  .site-header__nav a {
    color: var(--color-text-soft);
  }
  .site-header__nav a:hover {
    color: var(--color-accent);
    text-decoration: none;
  }

  @media (max-width: 860px) {
    .site-header__nav { gap: var(--space-3); font-size: 0.78rem; }
    .site-header__nav a:nth-child(n+4) { display: none; }
  }
</style>
